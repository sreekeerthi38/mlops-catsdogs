# Runtime + ML dependencies, version-pinned for reproducibility (M2).
# NOTE on torch: install the CPU build separately so you don't pull CUDA wheels:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
# (The Dockerfile and CI already do this.) Then: pip install -r requirements.txt
torch==2.5.1
torchvision==0.20.1

# Serving
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
pillow==11.0.0
numpy==1.26.4

# Experiment tracking + data versioning (M1)
mlflow==2.19.0
dvc==3.58.0

# Training utilities
scikit-learn==1.5.2
matplotlib==3.9.3
pyyaml==6.0.2

# Monitoring (M5)
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.0

# HTTP client for smoke/perf scripts
requests==2.32.3
